# Databricks Connect Configuration
# Copy this file to .env.local and fill in your values
# .env.local is git-ignored and will not be committed

# Databricks workspace configuration
DATABRICKS_HOST=https://your-workspace.azuredatabricks.net
DATABRICKS_TOKEN=your-personal-access-token-here

# Optional: Specify a cluster ID for databricks-connect
# If not set, will use serverless compute
# DATABRICKS_CLUSTER_ID=your-cluster-id

# Optional: Specify a SQL warehouse ID for SQL operations
# DATABRICKS_WAREHOUSE_ID=your-warehouse-id

# Optional: Databricks profile to use (alternative to env vars)
# DATABRICKS_CONFIG_PROFILE=dev
