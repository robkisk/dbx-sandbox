# This is a Databricks asset bundle definition for dbx_template.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_template
  uuid: 1f6ded8e-7534-4849-977b-da575773b83e

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2065124280106945.5.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-35466730673604.4.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/robby.kiskanyan@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/robby.kiskanyan@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: robby.kiskanyan@databricks.com
        level: CAN_MANAGE
